# v1.8 Selektor 4-Tujuan Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship v1.8.0 route selection tabs for cheapest, fastest, least-walk, and surprise routes.

**Architecture:** Keep v1.5 Pareto search as the surprise pool. Add one shared shortest-path search that accepts an objective (`fare`, `time`, `walk`) and deterministic tie-breaks, mirrored in `web/router.js` and `route.py`.

**Tech Stack:** Plain JavaScript, Python stdlib, static PWA.

## Global Constraints

- `route.py` and `web/router.js` must stay semantically identical for deterministic route results.
- TDD: write failing tests in `test-router.js` before production code.
- No new `data.json` fields; use `fare`, `etime`, and `xdist`.
- Web paths stay relative for `/jakarta-transit/`.
- Release docs must sync: `CHANGELOG.md`, `docs/ROADMAP.md`, `AGENTS.md`, and `README.md`.

---

### Task 1: Deterministic Goal Routes

**Files:**
- Modify: `test-router.js`
- Modify: `web/router.js`
- Modify: `route.py`

**Interfaces:**
- Produces: `findGoalRoutes(data, originName, destName, index)` in JS.
- Produces: `find_goals(origin, dest, data)` in Python.
- Output keys: `fare`, `time`, `walk`, each route shaped like existing `{transfers, stops, path}` plus `goalCost`.

- [x] Add RED tests for cheapest, fastest, least-walk, and tie-break behavior.
- [x] Run `node test-router.js` and confirm the new tests fail because goal routing is missing.
- [x] Implement the smallest shared Dijkstra objective search in JS.
- [x] Mirror the same search in Python.
- [x] Run `node test-router.js` and `python route.py A C` style selftests.

### Task 2: Selector UI

**Files:**
- Modify: `web/app.js`
- Modify: `web/index.html`
- Modify: `web/sw.js`

**Interfaces:**
- Consumes: `findGoalRoutes(...)`.
- Produces: 2x2 tabs with fixed labels: `Tarif terendah`, `Waktu tercepat`, `Minim jalan-kaki`, `Kejutan (beta)`.

- [x] Add RED static checks for `APP_VERSION`, four labels, and SW version source.
- [x] Render goal routes instead of old Pareto labels.
- [x] Keep surprise UI-only from non-winning Pareto pool; hide if no candidate exists.
- [x] Add `APP_VERSION = "1.8.0"` in `app.js`; expose it in the title badge and SW cache naming.

### Task 3: Release Sync

**Files:**
- Modify: `CHANGELOG.md`
- Modify: `docs/ROADMAP.md`
- Modify: `AGENTS.md`
- Modify: `README.md`

- [x] Mark v1.8.0 shipped in roadmap and current status docs.
- [x] Add changelog entry for v1.8.0.
- [x] Verify all local tests.
- [ ] Commit, push, and check the live Pages URL.
